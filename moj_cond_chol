% calculates conditional number using Cholesky decomposition
function cond=moj_cond_chol(A)
a=wlasna_max(A); % largest eigenvalue
b=wlasna_min_chol(A); % smallest eigenvalue
if b == 0
    cond = inf;
else
cond = a/b;
end
end
